---
tags: [学习资源, 平台费用, 平台费用检查, 京东]
---

# 平台费用检查 - 京东 - 保险费明细 - 账单归类模板

1. 平台费用名称不能重复
2. 账单归类名称不能重复。例外情况：不同账单但相同业务，一般不共用规则，此时账单归类名称可以一致。
3. 图例：`🔸 ` = 匹配字段，`🔹 ` = 默认提取订单号字段
4. 匹配时使用带 `🔸 ` 的列 + 收支方向
5. 如有 `🔹 ` 列，默认从该列提取订单号；必要时也可按规则从 `🔸 ` 列提取

### 保险费明细
1. 会从「订单id」中提取订单号（tid）。

#### 账单归类
| 账单归类名称 | 匹配规则（默认产品名称） | 🔸 产品名称 | 原始记录 | 费用说明 |
| --- | --- | --- | --- | --- |
| {账单归类名称} | {匹配规则} | {产品名称} | {原始记录} | {费用说明} |
